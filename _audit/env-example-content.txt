# Coach OS — variables d'environnement
#
# Ce fichier est du CONTENU pour `.env.example`, pas `.env.example` lui-même.
# Mon environnement d'exécution bloque toute écriture sur un chemin qui
# matche le motif `.env*` — même un fichier qui ne contient que des
# placeholders, sans aucune valeur réelle. C'est une règle de sandbox de
# l'agent, pas un choix arbitraire : voir _audit/FIX_CI.md, section
# « Ce que je n'ai pas su faire ».
#
# Pour finaliser, un humain (ou un agent sans cette contrainte) exécute :
#   cp _audit/env-example-content.txt .env.example
# puis supprime ce commentaire d'en-tête si souhaité.
#
# Copier `.env.example` en `.env.local` (variables `VITE_*`, lues côté client
# par Vite) et/ou exporter les variables `process.env.*` dans le shell qui
# lance les outils Node (`_runtime/`, `cli/`, `src/lib/tooling/`).
#
# AUCUNE valeur réelle ici — uniquement des placeholders. Ne jamais committer
# un `.env.local` rempli ; il est déjà exclu par `.gitignore`.
#
# Repéré par une recherche exhaustive de `import.meta.env.*` et
# `process.env.*` dans `src/**` (voir `_audit/FIX_CI.md` pour la méthode).

# ── Supabase (backend Postgres, RLS multi-tenant) ───────────────────────────
# Lues dans src/lib/authProviders.ts et src/lib/supabase.ts (via src/lib/env.ts).
# Sans elles, `supabaseConfigured` vaut faux et l'app bascule sur le seed
# local (mode démo) au lieu d'échouer en silence — voir le commentaire dans
# src/lib/supabase.ts sur le remplacement statique de `import.meta.env.VITE_*`
# par Vite : ces deux clés doivent rester des accès littéraux, jamais
# indirects (`env[cle]`), sous peine de disparaître du bundle au build.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ── Observabilité (RGPD opt-in) ─────────────────────────────────────────────
# Lues dans src/lib/observability.ts. Absentes = SDKs chargés mais inertes
# (aucune capture tant que l'utilisateur n'a pas donné son consentement).
VITE_POSTHOG_KEY=phc_your-posthog-project-key
# Optionnel — défaut : https://eu.posthog.com
VITE_POSTHOG_HOST=https://eu.posthog.com
VITE_USERTOUR_TOKEN=your-usertour-token
# Optionnel — id du contenu de tour à lancer au premier `first_window_open`
# (src/lib/windowOpenTracker.ts). Vide = no-op silencieux.
VITE_USERTOUR_CONTENT_ID=
# Optionnel — défaut au build : 'false'. Ne remplace pas le consentement
# persisté en localStorage, seulement le défaut avant tout choix explicite.
VITE_OBSERVABILITY_OPT_IN=false

# ── Intégrations ponctuelles ─────────────────────────────────────────────
# Lue dans src/apps/legal/ProboAnchor.tsx (ancrage iframe Probo, app Legal).
# Optionnel — défaut : chaîne vide (le cadre affiche pourquoi il est vide,
# jamais un cadre blanc silencieux).
VITE_PROBO_URL=

# ── Outillage Node (_runtime/, cli/, src/lib/tooling/) ──────────────────────
# Ces variables ne sont PAS `VITE_*` : elles sont lues par `process.env`, donc
# uniquement côté Node (scripts, CLI, harness généré), jamais embarquées dans
# le bundle client.

# Racine du corpus OKF sur disque. Lue dans src/lib/tooling/corpusStore.ts.
# Sans elle, les outils qui en dépendent lèvent une erreur explicite
# (CorpusIndisponible) plutôt que de lire un mauvais chemin par défaut.
# Ces outils ne fonctionnent que sur les surfaces CLI, harness ou MCP local.
ASPACE_CORPUS_ROOT=

# Active le mode démo (identité de test au lieu d'une identité réelle
# vérifiée). Lue dans src/lib/tooling/identity.ts. Valeur attendue : '1'.
# Ne jamais activer en production.
COACH_OS_DEMO_MODE=

# Identité de l'agent/process qui appelle l'outillage (harness généré,
# src/lib/tooling/adapters/harness.ts). Ne vient jamais du modèle — c'est le
# processus hôte qui les fixe, jamais une valeur que l'agent pourrait
# s'attribuer lui-même.
COACH_OS_TENANT=
COACH_OS_ACTOR=
COACH_OS_ROLE=

# Répertoire où les outils d'écriture déposent leurs propositions (jamais une
# écriture réelle directe). Lue dans src/lib/tooling/serverStore.ts. Défaut
# interne si absente — ne pas définir en local sauf besoin explicite de
# rediriger les propositions ailleurs que le chemin par défaut du dépôt.
COACH_OS_PROPOSAL_DIR=
