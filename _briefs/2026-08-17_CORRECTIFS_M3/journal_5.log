Périmètre respecté : mes fichiers sont `api/v1/tools.ts`, `api/v1/[tool].ts`, `api/v1/module-load.test.ts` (nouveau), `package-lock.json`, et le rapport `RAPPORT_FIX_5.md`. Aucune touche à `src/`.

**FIX_5 livré.**

**1. 500 sur `/api/v1/*`** — corrigé sans toucher `src/`. Les deux routes `api/v1/tools.ts` et `api/v1/[tool].ts` faisaient un `import { registerAll } from '../../src/lib/tooling/catalog'` au top-level, ce qui déclenche `catalog/index.ts:21 registerAll()` → `saasBuilder.ts` → `useThreeAppStore` (zustand + `localStorage`) → `ReferenceError` en Node. Le garde (`verifierAcces`) ne pouvait rien : le module crashe avant l'invocation du gestionnaire. Correctif : retirer l'import fautif du top-level, différer le chargement du catalogue **dans le gestionnaire**, après le garde, en `try/catch`. Échec du chargement → 503 explicite avec un message pointant vers `src/`. Garde reste fermé par défaut.

**2. Vérification obligatoire du brief** — `verifierAcces` rend bien **503** pour les requêtes non authentifiées en prod (`api/_agent/garde.test.ts` 20/20 verts). Aucune porte ouverte.

**3. Verrou module-load** — nouveau `api/v1/module-load.test.ts` avec `// @vitest-environment node`. Échoue sur l'ancien code (ReferenceError), passe sur le nouveau.

**4. Dépendances vulnérables** — `npm audit fix` (sans `--force`) :
- `postcss` 8.5.21 → **8.5.26** (devDep direct)
- `nanoid` 3.3.16 → **3.3.18** (transitif via postcss)
- `dompurify` 3.4.12 → **3.4.13** (transitif via posthog-js)

`package.json` intact (la plage `^8.5.21` absorbait le bump). Seul `package-lock.json` a bougé.

**5. `vite.config.ts`** — vérifié : aucun `define`, aucun `envPrefix`, le `devApiPlugin` ne pousse rien vers `import.meta.env`. Sain.

**6. Mesures autorisées** — `npm run build` OK (4.59 s), `npx vitest run api/` **22/22 verts**, `npm audit` **0 vulnérabilité**.

**7. Cause racine (hors périmètre, §3 du rapport)** — vit dans `src/stores/threeApp.store.ts` et `src/lib/saas-builder/ledger.store.ts` (`createJSONStorage(() => localStorage)` sans garde). Trois options documentées (lazy imports / split catalog / shim localStorage) pour l'agent `src/`.

Rapport complet : `_briefs/2026-08-17_CORRECTIFS_M3/RAPPORT_FIX_5.md`.
