Toutes mes tâches sont terminées. Récapitulatif final.

**Périmètre respecté** : 5 fichiers `api/` modifiés + 1 rapport créé. Aucun fichier hors-périmètre touché.

**Modifications** :
- `api/v1/[tool].ts` — `verifierAcces` en tête du gestionnaire, gestionnaire exporté pour les tests
- `api/v1/tools.ts` — idem
- `api/agent/roster.ts` — idem
- `api/agent/providers.ts` — idem
- `api/_agent/garde.test.ts` — 10 nouveaux tests (les 10 existants passent toujours, total 20/20 verts à ma première exécution)
- `_briefs/2026-08-17_CORRECTIFS_M3/RAPPORT_FIX_3.md` — rapport complet

**Décision pour roster/providers** : les deux gardés avec `verifierAcces` — `roster` est appelé seulement après login (AssistantOverlay, AssistantSettings, tous deux post-Desktop), `providers` n'a aucun appelant côté client (recherche `src/**` exhaustive, 0 résultat). Garde conforme à `/api/chat` et `/api/agent/invoke`.

**Diagnostic du 500** (non corrigé, hors périmètre) : `api/v1/{tools,[tool]}.ts` exécutent `registerAll()` au chargement du module (side-effect top-level). La chaîne d'import tire `useThreeAppStore` (`src/stores/threeApp.store.ts`) et `useLedgerStore` (`src/lib/saas-builder/ledger.store.ts`) — tous deux Zustand `persist` + `createJSONStorage(() => localStorage)`. `localStorage` n'est pas défini en Node serverless, donc l'évaluation jette → 500 `FUNCTION_INVOCATION_FAILED`. Hypothèse du brief voisine mais pas exacte : ce n'est pas la résolution de modules qui échoue, c'est l'exécution des side-effects au top-level. Cause exacte et deux pistes de correction documentées dans le rapport.

**Note sur la mesure** : à ma première exécution, 20/20 verts. Une seconde exécution quelques minutes plus tard échoue au chargement (`Failed to resolve import "../auth/storage-scope" from "src/stores/threeApp.store.ts"`) parce qu'un autre agent refactore actuellement ce fichier — ses imports `../auth/...` ne pointent pas encore vers les fichiers déplacés dans `src/lib/auth/`. Pas mon périmètre ; noté dans le rapport.
